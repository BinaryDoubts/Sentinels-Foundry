{{!-- List of Applied Mods --}}
{{#if selectedmods.length}}
<div class="box-border mod-box" style="border: 2px solid {{coloring}}">
    {{#if (eq bonus true)}}<p>{{localize "SCRPG.mod.bonus"}}</p>{{/if}}
    <div class="mod-bonus">
        {{#each selectedmods}}
        {{#if (gte data.data.value 0)}}
        <div class="green-abilities mod-rolls">
            <span class="mod-roll-text"> {{data.data.value}} {{#if data.data.named}}{{data.name}}{{/if}}</span>
            <div class="p-e-icon">
                {{#if (eq data.data.persistant true)}}
                <i title='{{localize "SCRPG.mod.persistant"}}' class="fas fa-hourglass"></i>
                {{/if}}
                {{#if (eq data.data.exclusive true)}}
                <i title='{{localize "SCRPG.mod.exclusive"}}' class="fas fa-lock"></i>
                {{/if}}
            </div>
        </div>
        {{/if}}
        {{/each}}
    </div>
    {{#if (eq penalty true)}}<p>{{localize "SCRPG.mod.penalty"}}</p>{{/if}}
    <div class="mod-penalty">
        {{#each selectedmods}}
        {{#if (lte data.data.value 0)}}
        <div class="red-abilities mod-rolls">
            <span class="mod-roll-text"> {{data.data.value}} {{#if data.data.named}}{{data.name}}{{/if}}</span>
            <div class="p-e-icon">
                {{#if (eq data.data.persistant true)}}
                <i title='{{localize "SCRPG.mod.persistant"}}' class="fas fa-hourglass"></i>
                {{/if}}
                {{#if (eq data.data.exclusive true)}}
                <i title='{{localize "SCRPG.mod.exclusive"}}' class="fas fa-lock"></i>
                {{/if}}
            </div>
        </div>
        {{/if}}
        {{/each}}
    </div>
</div>
{{/if}}